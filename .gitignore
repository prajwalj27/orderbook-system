# ── C++ Engine ────────────────────────────────────────────────
engine/build/
engine/results/
*.exe
*.o
*.a
*.dll

# ── Maven (gateway, consumer, client) ─────────────────────────
target/
*.class
*.jar

# ── Runtime data ───────────────────────────────────────────────
# WAL file — generated at runtime, not source code
gateway/data/

# ── Credentials ────────────────────────────────────────────────
# consumer/application.properties contains PlanetScale credentials.
# Keep the file tracked but never commit real credentials.
# Use placeholder values (YOUR_PLANETSCALE_USERNAME etc.) in the repo.

# ── Python tests ───────────────────────────────────────────────
__pycache__/
*.pyc
.pytest_cache/

# ── IDE ────────────────────────────────────────────────────────
.idea/
*.iml
.vscode/
.classpath
.project
.settings/
*.suo
*.user

# ── OS ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
